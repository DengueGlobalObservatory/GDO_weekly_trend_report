# BLOCKED — GDO internal news report, 2026-08-12

**Outcome:** Step 1 (fetch) failed. No `latest_status.csv`/`movers.csv` were
produced for this run, so no report numbers exist to write. Per
`AGENT_PROMPT.md` Step 1 ("if it failed, stop and file a BLOCKED report
rather than fabricating data"), this run stops here. No numeric claims were
invented; no `reports/2026_08_12_gdo_internal_news_report.md` was written.

## Step 0 — environment probe

- `Rscript` was not on `PATH` at the start of this run. Self-install
  succeeded (single attempt, per Step 0): `apt-get install -y
  --no-install-recommends r-base-core` → R 4.3.3 (2024-02-29). Required
  packages (`httr`, `jsonlite`, `dplyr`, `readr`, `tibble`) were also
  missing and were installed via `apt-get install r-cran-httr r-cran-jsonlite
  r-cran-dplyr r-cran-readr r-cran-tibble` (all resolved cleanly).
- `gh` CLI is not on `PATH`. Not needed — GitHub operations for *this* repo
  go through the `mcp__github__*` tools, which work: `mcp__github__get_me`
  succeeded (authenticated as `KMSusong`).
- `git push` to `origin main`: **works**. This resolves the write-access gap
  documented in README.md's "Known infrastructure gaps" #2 from the
  2026-08-11 test run (that run saw `git push` 403 and
  `mcp__github__push_files`/`mcp__github__issue_write` both 403). This run
  fast-forwarded `main` to include that prior run's unpushed commit
  (`9c93970`) and confirmed `git push -u origin main` succeeds
  (`origin/main` already had it — so a human or a subsequent fix already
  landed it). Treat gap #2 as resolved; see README update in this commit.

## Step 0.5 — data-pull check

- `target_render_date` = 2026-08-04 (most recent of {this month's 4th
  (2026-08-04), this month's 18th (2026-08-18, not strictly before today),
  last month's 18th (2026-07-18)} strictly before 2026-08-12).
- `snapshots/` contains no dated folders — this is the **first pull ever**,
  so per AGENT_PROMPT.md this is a pull week regardless of the date check.

## Step 1 — fetch (this is where it failed)

`Rscript R/run_weekly_pipeline.R` errored in `fetch_snapshot()` →
`list_output_folder_dates()`:

```
Error in list_output_folder_dates() :
  list_output_folder_dates(): GitHub Contents API returned 403 for
  https://api.github.com/repos/DengueGlobalObservatory/DENV_global_observatory/contents/Output
```

Diagnosis (a **new** infrastructure gap, distinct from the 2026-08-11
findings):

1. First 403: `DengueGlobalObservatory/DENV_global_observatory` (the public
   upstream data repo) was outside this session's GitHub-tool scope. Fixed
   by attaching it (`add_repo`, `access: push`, needed because read-only
   `access: read` only covers anonymous `git clone`/`fetch`, not
   `api.github.com` calls).
2. After attaching, the same endpoint still 403s, now with a different,
   org-level message: **"GitHub access is not enabled for this session. An
   org admin must connect the Claude GitHub App for this organization."**
   This requires a human with org-admin rights on `DengueGlobalObservatory`
   to connect/authorise the Claude GitHub App — nothing available to this
   routine can work around it.

Confirmed this is specifically an API-authorisation gap, not a data
problem: unauthenticated `raw.githubusercontent.com` access to the actual
target file works fine —
`https://raw.githubusercontent.com/DengueGlobalObservatory/DENV_global_observatory/main/Output/2026_08_04/DENV_cases_nowcast_output.csv`
returns 200 with the expected CSV (confirming `Output/2026_08_04/` exists,
matching `target_render_date`). Only the GitHub **Contents API** (folder
listing, used to resolve the fallback-to-nearest-earlier-date logic) and
**Commits API** (commit-SHA provenance for `manifest.json`) are blocked,
because `R/fetch_snapshot.R` calls both via `httr::GET()` to
`api.github.com`, which routes through this sandbox's proxy and hits the
org-level gate above.

## What's needed to unblock

- An org admin connects the Claude GitHub App for `DengueGlobalObservatory`
  (same fix category as gap #2, but this is `api.github.com` Contents/Commits
  access, not repo write access — check the org's installed GitHub Apps /
  connection settings).
- No code change is required in this repo to fix the root cause. (A more
  defensive alternative — having `R/fetch_snapshot.R` enumerate `Output/`
  folder dates and commit SHAs via `git ls-remote`/shallow-clone plumbing
  instead of the Contents/Commits REST API, since anonymous git reads of
  public repos do work in this sandbox — is a legitimate future option, but
  is a change to the deterministic pipeline script and is left for a human
  to decide on, not made unilaterally by this routine.)

## Steps 2–6

Not run — no snapshot data exists to read (first pull ever, and the only
fetch attempt failed).

## Steps 7–8

This BLOCKED report and a README update documenting this gap are being
committed to `main`, and a GitHub Issue is being filed, since both those
write paths are confirmed working this run.
